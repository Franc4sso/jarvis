# JARVIS uses the Claude Code CLI as its brain — no API key needed here.
# Make sure `claude` is installed and logged in (run `claude` once interactively).
PORT=8787

# Optional overrides:
# CLAUDE_BIN=claude
# JARVIS_MODEL=claude-opus-4-8
